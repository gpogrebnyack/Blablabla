# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Swift Package Manager (Package.resolved is committed so builds are reproducible)
.build/
.swiftpm/

# Benchmarks — kept locally for our own tuning, not part of the public repo.
benchmark/

# Editor temp files
*.swp
*~

# Landing page — developed separately, never shipped with app releases.
landing/
